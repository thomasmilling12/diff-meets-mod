# Copy this file to pi/.env and fill in your values.
# This file is loaded by the systemd service on your Pi.

# Required: Your Discord bot token
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Required: Port for the Express health server + web dashboard
PORT=3000

# Optional: Path to the SQLite database file (default: ./data/bot.db)
DB_PATH=/home/pi/diff-meets-mod/data/bot.db

# Optional: Node environment
NODE_ENV=production

# Optional: Secret token to protect the web dashboard.
# Access the dashboard at http://<pi-ip>:3000/dashboard?token=YOUR_TOKEN
# Leave blank to make the dashboard public (not recommended).
DASHBOARD_TOKEN=your_secret_dashboard_token_here
